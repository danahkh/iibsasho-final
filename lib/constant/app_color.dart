import 'package:flutter/material.dart';

class AppColor {
  static const Color primary = Color(0xFF242476);
  static const Color primarySoft = Color(0xFFEAEAF2);
  static const Color secondary = Color(0xFF0A0E2F);
  static const Color accent = Color(0xFFFABA3E);
  static const Color border = Color(0xFFD3D3E4);
  static const Color background = Color.fromARGB(255, 242, 255, 231);

  // Accessibility improvements
  static const Color textDark = Color(0xFF333333); // For text on navy/primary backgrounds
  static const Color textLight = Color(0xFFFFFFFF); // For text on dark backgrounds/buttons
  static const Color textBlack = Color(0xFF000000); // For text on off-white backgrounds
  static const Color placeholder = Color(0xFF555555); // For placeholder/hint text
}
