{
  "version": 2,
  "builds": [
    { "src": "package.json", "use": "@vercel/static-build", "config": { "distDir": "build/web" } }
  ],
  "env": {
    "FLUTTER_WEB_AUTO_DETECT": "true"
  },
  "ignoreCommand": "echo 'Using all files'"
}
